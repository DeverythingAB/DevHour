name: DevHour pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 3-Configure-ci-cd-pipelines
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'adopt'
      - run: mvn clean install
      - uses: actions/upload-artifact@v3
        with:
          name: DevHour-build
          path: |
            3-Configure-ci-cd-pipelines/
  package:
    needs: build
    runs-on : ubuntu-latest
    steps:
      - uses: actions/download-artifact@v3
        id: build
        with:
          name: DevHour-build
        shell: bash
      - name: Git hash
        id: git-hash
      - run: |
          docker build -t order-service:${{ github.sha }} ./order-service
          docker build -t product-service:${{ github.sha }} ./product-service
          docker build -t report-service:${{ github.sha }} ./report-service
        shell: bash


#  test:
#    needs: package
#    runs-on: ubuntu-latest
#    defaults:
#      run:
#        working-directory: 3-Configure-ci-cd-pipelines
#    steps:
#      - uses: actions/download-artifact@v3
#        with:
#          name: DevHour-jars
#          path: 3-Configure-ci-cd-pipelines/staging/*.jar