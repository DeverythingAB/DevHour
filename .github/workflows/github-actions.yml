name: DevHour pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 3-Configure-ci-cd-pipelines
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'adopt'
      - run: mvn clean install
      - uses: actions/upload-artifact@v3
        with:
          name: DevHour-build
          path: |
            3-Configure-ci-cd-pipelines/
  package:
    needs: build
    runs-on : ubuntu-latest
    steps:
      - uses: actions/download-artifact@v3
        id: build
        with:
          name: DevHour-build
      - run: cd ${{steps.build.outputs.download-path}} && ls -R
        shell: bash
      - run: |
          docker build -t order-service ./3-Configure-ci-cd-pipelines/order-service
          docker build -t product-service ./3-Configure-ci-cd-pipelines/product-service
          docker build -t report-service ./3-Configure-ci-cd-pipelines/report-service
        shell: bash


#  test:
#    needs: package
#    runs-on: ubuntu-latest
#    defaults:
#      run:
#        working-directory: 3-Configure-ci-cd-pipelines
#    steps:
#      - uses: actions/download-artifact@v3
#        with:
#          name: DevHour-jars
#          path: 3-Configure-ci-cd-pipelines/staging/*.jar